[2025-02-25 20:25] - Implementação: Notificações em Tempo Real no Frontend (Site)
Descrição: 
- Criado utilitário de som `src/lib/audio.ts` (beep simples via Web Audio API).
- Atualizado `DashboardPage.tsx` (Painel do Dono) para:
  - Escutar eventos `INSERT` e `UPDATE` na tabela `agendamentos`.
  - Atualizar a lista de agendamentos em tempo real (sem precisar recarregar).
  - Tocar som de notificação ao receber novo agendamento.
  - Exibir toast de sucesso e notificação do navegador (se em background).
- Atualizado `FuncionarioAgendaPage.tsx` (Agenda do Funcionário) para tocar som de notificação (já possuía atualização de lista).
Impacto: Usuários agora recebem feedback visual e sonoro imediato ao receberem novos agendamentos enquanto estão com o site aberto.
Testes Realizados: Validação lógica do código.
Dados Utilizados: Código fonte.
Responsável: Agente IA
Próximos Passos: Usuário testar recebimento de agendamento com o painel aberto.
